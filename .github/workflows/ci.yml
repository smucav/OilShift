name: FraudPulse CI

on:
  push:
    branches:
      - main
      - task-*

jobs:
  check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'
      - name: Install dependencies
        run: |
          pip install -r requirements.txt
      - name: Lint code
        run: |
          echo ✅ Code check placeholder — passes for now.

